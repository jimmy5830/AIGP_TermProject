# ============================================================
# ML-Agents Python 환경 (Python 3.10 필수, 3.14 사용 불가)
# 가상환경 위치: D:\GitHub\AIGP_TermProject\venv_mlagents\
#
# 설치 방법:
#   py -3.10 -m venv D:\GitHub\AIGP_TermProject\venv_mlagents
#   cd D:\GitHub\AIGP_TermProject\venv_mlagents\Scripts
#   python -m pip install numpy==1.23.5
#   python -m pip install mlagents==0.30.0 mlagents-envs==0.30.0 --no-deps
#   python -m pip install -r requirements-mlagents.txt --no-deps (아래 패키지)
# ============================================================

# Core ML-Agents (PyPI 최신 stable, Python 3.10 호환)
# numpy는 --no-deps로 먼저 설치 후 나중에 1.23.5로 고정할 것
mlagents==0.30.0
mlagents-envs==0.30.0

# PyTorch (Python 3.10 wheel 있음)
torch==2.2.2

# numpy: 반드시 1.22.x~1.23.x 사용
# - 1.21.x: Python 3.10 wheel 없음 (소스 빌드 시 cp949 인코딩 오류)
# - 1.24+: np.float 제거됨 → mlagents 0.30.0 AttributeError 발생
numpy==1.23.5

# ONNX export
onnx==1.15.0

# TensorBoard
tensorboard==2.13.0

# Runtime dependencies (버전 범위 지정)
PyYAML>=6.0
grpcio>=1.47.0
cloudpickle>=2.1.0
h5py>=3.7.0
pillow>=9.0.0
tqdm>=4.64.0
attrs>=21.4.0
cattrs>=1.1.0,<1.7
pypiwin32==223
gym>=0.26.2
PettingZoo==1.15.0
